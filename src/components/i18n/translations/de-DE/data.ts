import { Translation, RegexTranslation } from '../../types'

export const map = new Map<string, Translation>([
  /* Your translation here */
  ['*', [
    /* CSS translation here */
  ]],
])
export const regex: RegexTranslation = [
  /* Regex translation here */
]
